name: Call Reusable Workflow

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
jobs:
  call-reusable:
    uses: ecmwf-projects/cads-obs-catalogue-manager/.github/workflows/on-push.yml@main
    secrets:
      TOKEN: ${{ secrets.REUSABLE_WORKFLOW_PAT }}